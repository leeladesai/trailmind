# FastAPI & Web
fastapi==0.104.1
jinja2==3.1.2
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# LLM & AI (Mesh API — OpenAI-compatible)
openai==1.10.0
langgraph==0.0.20
langsmith==0.0.87

# Vector Database
# Pinned explicitly, not left to transitive resolution: chromadb==0.4.24 (2023) still
# uses the now-removed np.float_/np.int_ aliases, which break under NumPy 2.x. Local
# dev never hit this because uv.lock happened to have resolved numpy 1.26.4 already;
# `pip install -r requirements.txt` (used by the Render deploy) has no such lock to
# fall back on and would otherwise pull latest NumPy 2.x.
numpy==1.26.4
chromadb==0.4.24

# Database
sqlalchemy==2.0.23
alembic==1.13.1
# Postgres driver (Neon in prod — see render.yaml DATABASE_URL). psycopg2, not psycopg3,
# so Neon's plain "postgresql://..." connection string works with SQLAlchemy's default
# dialect as-is, no "+psycopg" URL rewriting needed. Binary wheel avoids needing
# libpq/pg_config on the build image. Local dev still defaults to SQLite, so this only
# actually connects to anything once DATABASE_URL is overridden.
psycopg2-binary==2.9.9

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6

# Utilities
python-dotenv==1.0.0
httpx==0.25.2
# DOM-scrape catalog ingestion adapter (ING-2) — stdlib html.parser backend, no lxml
# needed.
beautifulsoup4==4.12.3

# Scheduling (bonus)
APScheduler==3.10.4

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Development
black==23.12.0
flake8==6.1.0
